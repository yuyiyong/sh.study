/*import styles from './setting/rowMy.less'
export const RowMy = (props) => {
  return (
    <div className={styles.row}>
      {props.children}
    </div>
  )
};

export const ColMy = (props) => {
  let width = props.md?props.md+"%":'25%'
  console.log("width:",width);
  return (
    <div
      className={styles.col}
      style={{width:width}} //数值是否可以直接传入到css中, md/sm这些如何用
    >
      {props.children}
    </div>
  )

}*/
